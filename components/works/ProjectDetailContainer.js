export default function ProjectDetailContainer({ children }) {
    return (
        <div class="flex border-t border-gray-200  justify-between py-2 items-center">
            {children}
        </div>
    )
}
